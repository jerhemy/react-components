/**
 * Export the BarGraph component and its types from the BarGraph directory
 */

import BarGraph, { BarGraphDataPoint, BarGraphProps } from './BarGraph/BarGraph';

export default BarGraph;
export type { BarGraphDataPoint, BarGraphProps }; 