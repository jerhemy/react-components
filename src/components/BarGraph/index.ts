/**
 * Export the BarGraph component and its types
 */

import BarGraph, { BarGraphDataPoint, BarGraphProps } from './BarGraph';

export default BarGraph;
export type { BarGraphDataPoint, BarGraphProps }; 